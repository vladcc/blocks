#-n|--block-name=<matcher>
#-s|--block-start=<matcher>
#-e|--block-end=<matcher>
#-C|--comment=<matcher>
#-B|--block-comment-begin=<matcher>
#-T|--block-comment-terminate=<matcher>
#-m|--match=<matcher>
#-M|--dont-match=<matcher>
#-S|--mark-start=<string>
#-E|--mark-end=<string>
#-c|--block-count=<num>
#-k|--skip=<num>
#-l|--line-numbers
#-N|--with-filename
#-w|--files-with-match
#-W|--files-without-match

#-i|--case-insensitive
#-I|--ignore-top
#-F|--fatal-error

#-r|--regex-match
#-f|--fixed-match

#-h|--help
#-v|--version

context_arg_type prog_options
context_var_name context

unbound_arg_code
	prog_options * context = (prog_options *)ctx;
	context->file_names->push_back(arg);
end_code

on_error_code
#define eprintf(...) fprintf(stderr, __VA_ARGS__)

	eprintf("%s: error: ", program_name);
	switch (err_code)
	{
		case OPTS_UNKOWN_OPT_ERR:
			eprintf("option '%s' unknown\n", err_opt);
		break;
		case OPTS_ARG_REQ_ERR:
			eprintf("option '%s' requires an argument\n", err_opt);
		break;
		case OPTS_NO_ARG_REQ_ERR:
			eprintf("option '%s' does not take arguments\n", err_opt);
		break;
		default:
		break;
	}

	print_use();
	print_try();

	exit(EXIT_FAILURE);

#undef eprintf
end_code

long_name  block-name
short_name n
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->block_name = opt_arg;
	context->is_block_name_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Print only blocks starting with a name which matches <matcher>.");
printf("Default is '%s'\n", defaults.block_open);
puts("");
end_code
end

long_name  block-start
short_name s
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->block_start = opt_arg;
	context->is_block_start_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the open block symbol. Default is '%s'\n",
defaults.block_open
);
puts("");
end_code
end

long_name  block-end
short_name e
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->block_end = opt_arg;
	context->is_block_end_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the close block symbol. Default is '%s'\n",
defaults.block_close
);
puts("");
end_code
end

long_name  comment
short_name C
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->comment = opt_arg;
	context->is_comment_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the single line comment symbol. Default is '%s'\n",
defaults.line_comment
);
puts("");
end_code
end

long_name  block-comment-begin
short_name B
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->block_comment_begin = opt_arg;
	context->is_block_comment_begin_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the start of block comment symbol. Default is '%s'\n",
defaults.block_comment_begin
);
puts("");
end_code
end

long_name  block-comment-terminate
short_name T
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->block_comment_terminate = opt_arg;
	context->is_block_comment_terminate_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the end of block comment symbol. Default is '%s'\n",
defaults.block_comment_terminate
);
puts("");
end_code
end

long_name  match
short_name m
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->match = true;
	context->dont_match = false;
	context->match_dont_match = opt_arg;
	context->is_match_dont_match_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Print only blocks in which at least one line matches <matcher>.");
puts("");
end_code
end

long_name  dont-match
short_name M
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->match = false;
	context->dont_match = true;
	context->match_dont_match = opt_arg;
	context->is_match_dont_match_regex = context->is_next_matcher_regex;
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Print only blocks in which no line matches <matcher>.");
puts("");
end_code
end

long_name  mark-start
short_name S
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->mark_start = opt_arg;
end_code

help_code
printf("%s|%s <string>\n", short_name, long_name);
puts("When given, <string> will be printed before each block.");
puts("");
end_code
end

long_name  mark-end
short_name E
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->mark_end = opt_arg;
end_code

help_code
printf("%s|%s <string>\n", short_name, long_name);
puts("When given, <string> will be printed after each block.");
puts("");
end_code
end

long_name  block-count
short_name c
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	if (sscanf(opt_arg, "%d", &(context->block_count)) != 1)
		equit("option '%s': '%s' bad number", opt, opt_arg);
	if(context->block_count < 0)
		equit("option '%s': '%s' has to be positive", opt, opt_arg);
end_code

help_code
printf("%s|%s <num>\n", short_name, long_name);
puts("Print the first <num> number of blocks from the current file.");
puts("");
end_code
end

long_name  skip
short_name k
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	if (sscanf(opt_arg, "%d", &(context->skip_count)) != 1)
		equit("option '%s': '%s' bad number", opt, opt_arg);
	if(context->skip_count < 0)
		equit("option '%s': '%s' has to be positive", opt, opt_arg);
end_code

help_code
printf("%s|%s <num>\n", short_name, long_name);
puts("Don't print the first <num> number of blocks from the current file.");
puts("");
end_code
end

long_name  line-numbers
short_name l
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->line_numbers = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print line numbers with output lines.");
puts("");
end_code
end

long_name  with-filename
short_name N
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->with_filename = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print file name with output lines.");
puts("");
end_code
end

long_name  files-with-match
short_name w
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->files_with_match = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print only the names of files which contained at least one block.");
puts("");
end_code
end

long_name  files-without-match
short_name W
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->files_without_match = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print only the names of files which did not contain a block.");
puts("");
end_code
end

long_name  case-insensitive
short_name i
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->case_insensitive = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Match all matchers case insensitive.");
puts("");
end_code
end

long_name  ignore-top
short_name I
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->ignore_top = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print only the body of the block. I.e. don't print the block name,");
puts("block start, and block end. All lines between and including the block");
puts("name and block open, as well as the line containing the block end are");
puts("ignored.");
puts("");
end_code
end

long_name  fatal-error
short_name F
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->fatal_error = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Quit processing the current file after the first error.");
puts("");
end_code
end

long_name  regex-match
short_name r
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->is_next_matcher_regex = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("All matchers after this argument are regex. This is the default.");
puts("");
end_code
end

long_name  fixed-match
short_name f
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->is_next_matcher_regex = false;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("All matchers after this argument are fixed strings.");
puts("");
end_code
end

long_name  help
short_name h
takes_args false
handler_code
	help_message();
	opts_print_help((opts_table *)(ctx));
	exit(EXIT_SUCCESS);
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print this screen and quit.");
puts("");
end_code
end

long_name  version
short_name v
takes_args false
handler_code
	printf("%s v%s\n", program_name, program_version);
	puts("Author      : Vladimir Dinev");
	puts("Bug reports : vld.dinev@gmail.com");
	puts("Compile date: 2020-08-30");
	exit(EXIT_SUCCESS);
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print version info and quit.");
end_code
end
