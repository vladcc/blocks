defn_start

#-n|--block-name=<matcher>
#-s|--block-start=<matcher>
#-e|--block-end=<matcher>
#-C|--comment=<matcher>
#-B|--block-comment-begin=<matcher>
#-T|--block-comment-terminate=<matcher>
#-m|--match=<matcher>
#-M|--dont-match=<matcher>
#+a
#+o
#-S|--mark-start=<string>
#-E|--mark-end=<string>
#-c|--block-count=<num>
#-k|--skip=<num>
#-l|--line-numbers
#-N|--with-filename
#-w|--files-with-match
#-W|--files-without-match

#-i|--case-insensitive
#+i
#-A|--case-sensitive
#+A
#-I|--ignore-top
#-x|--no-defaults
#-F|--fatal-error
#-V|--verbose-error
#-D|--debug

#-g|--lang
#-d|--directory
#-R|--recursive
#-u|--include-files-rx
#-U|--exclude-files-rx
#-L|--file-list
#-z|--no-strings
# --string-rx

#-r|--regex-match
#+r
#-f|--fixed-match
#+f

#-h|--help
#-v|--version

context_arg_type prog_options
context_var_name context

unbound_arg_code
	opts_unbound_arg(arg, ctx);
end_code

on_error_code
#define eprintf(...) fprintf(stderr, __VA_ARGS__)

	eprintf("%s: error: ", program_name);
	switch (err_code)
	{
		case OPTS_UNKOWN_OPT_ERR:
			eprintf("option '%s' unknown\n", err_opt);
		break;
		case OPTS_ARG_REQ_ERR:
			eprintf("option '%s' requires an argument\n", err_opt);
		break;
		case OPTS_NO_ARG_REQ_ERR:
			eprintf("option '%s' does not take arguments\n", err_opt);
		break;
		default:
		break;
	}

	print_use(stderr);
	print_try();

	exit_err();

#undef eprintf
end_code

long_name  block-name
short_name n
takes_args true
handler_code
	handle_matcher(B_NAME, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Print only blocks starting with a name which matches <matcher>.");
printf("Default is '%s'\n", defaults.block_open);
puts("");
end_code
end

long_name  block-start
short_name s
takes_args true
handler_code
	handle_matcher(B_START, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the open block symbol. Default is '%s'\n",
defaults.block_open
);
puts("");
end_code
end

long_name  block-end
short_name e
takes_args true
handler_code
	handle_matcher(B_END, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
printf(
"Set <matcher> as the close block symbol. Default is '%s'\n",
defaults.block_close
);
puts("");
end_code
end

long_name  comment
short_name C
takes_args true
handler_code
	handle_matcher(B_LINE_COMMENT, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Set <matcher> as the single line comment symbol.");
puts("");
end_code
end

long_name  block-comment-begin
short_name B
takes_args true
handler_code
	handle_matcher(B_COMMENT_BEGIN, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Set <matcher> as the start of block comment symbol.");
puts("");
end_code
end

long_name  block-comment-terminate
short_name T
takes_args true
handler_code
	handle_matcher(B_COMMENT_TERM, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Set <matcher> as the end of block comment symbol.");
puts("");
end_code
end

long_name  match
short_name m
takes_args true
handler_code
	handle_matcher(MATCH, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Print only blocks in which at least one line matches <matcher>.");
puts("Does not ignore comments and strings.");
puts("If both match and don't match are given, logical and is applied even if");
puts("it is not specified on the command line. If a logical operation is");
puts("specified on the command line, both match and don't match must be give.");
puts("Logical operations:");
printf("+%c - Logical and. Both matchers must match.\n", mM_and);
printf("+%c - Logical or. One or the other matcher must match.\n", mM_or);
printf("+%c|+%c can appear anywhere on the command line.\n", mM_and, mM_or);
puts("");
end_code
end

long_name  dont-match
short_name M
takes_args true
handler_code
	handle_matcher(DONT_MATCH, opt_arg, ctx);
end_code

help_code
printf("%s|%s <matcher>\n", short_name, long_name);
puts("Print only blocks in which no line matches <matcher>.");
puts("Does not ignore comments and strings.");
puts("If both match and don't match are given, logical and is applied even if");
puts("it is not specified on the command line. If a logical operation is");
puts("specified on the command line, both match and don't match must be give.");
puts("Logical operations:");
printf("+%c - Logical and. Both matchers must match.\n", mM_and);
printf("+%c - Logical or. One or the other matcher must match.\n", mM_or);
printf("+%c|+%c can appear anywhere on the command line.\n", mM_and, mM_or);
puts("");
end_code
end

long_name  mark-start
short_name S
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->mark_start = opt_arg;
end_code

help_code
printf("%s|%s <string>\n", short_name, long_name);
puts("When given, <string> will be printed before each block.");
puts("");
end_code
end

long_name  mark-end
short_name E
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->mark_end = opt_arg;
end_code

help_code
printf("%s|%s <string>\n", short_name, long_name);
puts("When given, <string> will be printed after each block.");
puts("");
end_code
end

long_name  block-count
short_name c
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	if (sscanf(opt_arg, "%d", &(context->block_count)) != 1)
		equit("option '%s': '%s' bad number", opt, opt_arg);
	if(context->block_count < 0)
		equit("option '%s': '%s' has to be positive", opt, opt_arg);
end_code

help_code
printf("%s|%s <num>\n", short_name, long_name);
puts("Print the first <num> number of blocks from the current file.");
puts("");
end_code
end

long_name  skip
short_name k
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	if (sscanf(opt_arg, "%d", &(context->skip_count)) != 1)
		equit("option '%s': '%s' bad number", opt, opt_arg);
	if(context->skip_count < 0)
		equit("option '%s': '%s' has to be positive", opt, opt_arg);
end_code

help_code
printf("%s|%s <num>\n", short_name, long_name);
puts("Don't print the first <num> number of blocks from the current file.");
puts("");
end_code
end

long_name  line-numbers
short_name l
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->line_numbers = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print line numbers with output lines.");
puts("");
end_code
end

long_name  with-filename
short_name N
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->with_filename = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print file name with output lines.");
puts("");
end_code
end

long_name  files-with-match
short_name w
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->files_with_match = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print only the names of files which contained at least one block.");
puts("");
end_code
end

long_name  files-without-match
short_name W
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->files_without_match = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print only the names of files which did not contain a block.");
puts("");
end_code
end

long_name  case-insensitive
short_name i
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->are_all_matchers_icase = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("All matchers after this argument are case insensitive.");
printf("+%c\n", short_name[1]);
puts("Only the next matcher after this argument is case insensitive.");
puts("");
end_code
end

long_name  case-sensitive
short_name A
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->are_all_matchers_icase = false;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("All matchers after this argument are case sensitive.");
printf("+%c\n", short_name[1]);
puts("Only the next matcher after this argument is case sensitive.");
puts("");
end_code
end

long_name  no-defaults
short_name x
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->no_defaults = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Do not use defaults for matchers.");
puts("");
end_code
end

long_name  ignore-top
short_name I
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->ignore_top = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print only the body of the block. I.e. don't print the block name,");
puts("block start, and block end. All lines between and including the block");
puts("name and block open, as well as the line containing the block end are");
puts("ignored.");
puts("");
end_code
end

long_name  fatal-error
short_name F
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->fatal_error = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Quit after the first error.");
puts("");
end_code
end

long_name  verbose-error
short_name V
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->verbose_error = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print what was parsed along with the error message to stderr.");
puts("");
end_code
end

long_name  debug
short_name D
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->debug = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print debug info about matchers and quit.");
puts("");
end_code
end

long_name  lang
short_name g
takes_args true
handler_code
	handle_lang(opt_arg, ctx);
end_code

help_code
printf("%s|%s <name>\n", short_name, long_name);
puts("Select language defaults. <name> is case insensitive and one of:");
puts("C, awk, json, xml, info");
puts(
"If used, all block matchers given on the command line but the block name"
);
puts("are ignored. info is the boost ptree info. xml takes care to place the");
puts("block name inside the tag automatically. To see the resulting matchers");
puts("for each language, use the debug info option. Ignores the no defaults");
puts("option.");
puts("");
end_code
end

long_name  directory
short_name d
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->files_dir = opt_arg;
end_code

help_code
printf("%s|%s <dir>\n", short_name, long_name);
puts(
"Read all files from <dir>. By default, all entries are read, not only files.\n"
"To filter, use the include/exclude options. Processed after the files given \n"
"on the command line and before the file list option."
);
puts(
"If both the include and the exclude options are provided, a file name must\n"
"match the include and not match the exclude in order to be selected."
);
puts("");
end_code
end

long_name  recursive
short_name R
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->recursive = true;
	if (!context->files_dir)
		context->files_dir = ".";
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts(
"Do a recursive file search. If the directory option is not provided, the\n"
"start directory defaults to '.'"
);
puts("Symlinks to directories are followed.");
puts("");
end_code
end

long_name  include-files-rx
short_name u
takes_args true
handler_code
	handle_matcher(FILES_INCLUDE_RX, opt_arg, ctx);
end_code

help_code
printf("%s|%s <regex>\n", short_name, long_name);
puts(
"If a file name found by the directory option matches <regex>, the file is\n"
"processed. <regex> is matched against the whole path. I.e. to make sure only\n"
"'.txt' files are filtered in, use '\\.txt$'"
);
puts("");
end_code
end

long_name  exclude-files-rx
short_name U
takes_args true
handler_code
	handle_matcher(FILES_EXCLUDE_RX, opt_arg, ctx);
end_code

help_code
printf("%s|%s <regex>\n", short_name, long_name);
puts(
"If a file name found by the directory option matches <regex>, the file is\n"
"not processed. <regex> is matched against the whole path. I.e. to make sure\n"
"only '.txt' files are filtered out, use '\\.txt$'"
);
puts("");
end_code
end

long_name  file-list
short_name L
takes_args true
handler_code
	prog_options * context = (prog_options *)ctx;
	context->file_list = opt_arg;
end_code

help_code
printf("%s|%s <file>\n", short_name, long_name);
puts("Read a list of input files from <file>. Processed after the files given");
puts("on the command line and after the directory option.");
puts("");
end_code
end

long_name  no-strings
short_name z
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->no_strings = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Ignore strings when looking for matching patterns.");
puts("");
end_code
end

long_name string-rx
short_name \0
takes_args true
handler_code
	handle_matcher(STRING_RX, opt_arg, ctx);
end_code

help_code
printf("%s <regex>\n", long_name);
printf(
"The regex used for identifying strings. Default is %s\n",
defaults.string_rx
);
puts("Used only if the no strings option is turned on.");
puts("");
end_code
end

long_name  regex-match
short_name r
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->are_all_matchers_regex = true;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("All matchers after this argument are regex.");
printf("+%c\n", short_name[1]);
puts("Only the next matcher after this argument is regex.");
puts("");
end_code
end

long_name  fixed-match
short_name f
takes_args false
handler_code
	prog_options * context = (prog_options *)ctx;
	context->are_all_matchers_regex = false;
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("All matchers after this argument are fixed strings.");
printf("+%c\n", short_name[1]);
puts("Only the next matcher after this argument is fixed string.");
puts("");
end_code
end

long_name  help
short_name h
takes_args false
handler_code
	help_message();
	opts_print_help((opts_table *)(ctx));
	exit(EXIT_SUCCESS);
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print this screen and quit.");
puts("");
end_code
end

long_name  version
short_name v
takes_args false
handler_code
	printf("%s %s\n", program_name, program_version);
	exit(EXIT_SUCCESS);
end_code

help_code
printf("%s|%s\n", short_name, long_name);
puts("Print version info and quit.");
end_code
end

defn_end
